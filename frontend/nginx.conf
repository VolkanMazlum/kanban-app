server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Basic Auth is replaced by JWT frontend login and backend roles

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # API reverse proxy → backend container
    location /api/ {
        proxy_pass http://backend:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Connection '';
        # Nginx auth geçtikten sonra backend'e güvenilir header ekle
        proxy_set_header X-Internal-Auth $INTERNAL_SECRET;
    }

    # Health check - auth gerektirmesin (monitoring için)
    location /health {
        auth_basic off;
        proxy_pass http://backend:4000/health;
    }

    # React SPA - all other routes serve index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}